\RequirePackage{etex}
\documentclass[conference]{IEEEtran}
\usepackage[utf8]{inputenc}

\usepackage{amsmath, amsthm, amssymb, stmaryrd, enumitem, array}
\usepackage{tikz, tikz-network}
\usepackage{algorithm, algpseudocode}
\usepackage{graphicx}
\usepackage{float}
\usepackage{listings}
\usepackage{color}
\usepackage[multiple]{footmisc}
\usepackage{multirow}
\usepackage{booktabs}
\usepackage{cite}

\setlength{\textfloatsep}{6pt plus 1pt minus 2pt}
\setlength{\floatsep}{6pt plus 1pt minus 2pt}
\setlength{\dbltextfloatsep}{6pt plus 1pt minus 2pt}
\setlength{\dblfloatsep}{6pt plus 1pt minus 2pt}
\setlength{\intextsep}{6pt plus 1pt minus 2pt}
\setlength{\abovecaptionskip}{2pt}
\setlength{\belowcaptionskip}{0pt}
\setcounter{topnumber}{1}
\setcounter{bottomnumber}{1}
\setcounter{totalnumber}{2}
\setlist[itemize]{nosep}
\setlist[enumerate]{nosep}

\definecolor{dkgreen}{rgb}{0,0.6,0}
\definecolor{gray}{rgb}{0.5,0.5,0.5}
\definecolor{mauve}{rgb}{0.58,0,0.82}

\righthyphenmin=2
\sloppy

%------------------------------- New theorems

% These will be typeset in italics
\newtheorem{theorem}{Theorem}[section]
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{corollary}[theorem]{Corollary}

% These will be typeset in Roman
\theoremstyle{definition}
\newtheorem{definition}[theorem]{Definition}
\newtheorem{fact}[theorem]{Fact}
\newtheorem{conjecture}[theorem]{Conjecture}
\newtheorem*{problem}{Problem}
\newtheorem{example}{Example}

%------------------------------- Macros
\newcolumntype{C}[1]{>{\centering\let\newline\\\arraybackslash\hspace{0pt}}m{#1}}

\newcommand{\TT}{\mathcal{T}}
\newcommand{\QQ}{\mathcal{Q}}


%-------------------------------


\begin{document}

\title{ComNetX: Local Hierarchical Adaptation for Dynamic Community Detection}

\author{
\IEEEauthorblockN{Anonymous}
\IEEEauthorblockA{Anonymous} \\

\and
\IEEEauthorblockN{Anonymous}
\IEEEauthorblockA{Anonymous} \\

\and
\IEEEauthorblockN{Anonymous}
\IEEEauthorblockA{Anonymous} \\

\and
\IEEEauthorblockN{Anonymous}
\IEEEauthorblockA{Anonymous} \\

\and
\IEEEauthorblockN{Anonymous}
\IEEEauthorblockA{Anonymous} \\
}

\maketitle

\begin{abstract}
Community detection in dynamic graphs is commonly handled by full-snapshot
recomputation or by specialized dynamic algorithms tied to a particular
objective. We introduce ComNetX, a local hierarchical adaptation framework that
maintains a multi-level community state, expands the affected region of a batch
update, contracts affected communities into compact local subproblems, and
invokes an existing solver only on these instances. The design supports
topology-only methods, feature-aware GNN clustering models, and dynamic solvers
used as local backends. We evaluate ComNetX through a multi-backend study on
six real networks, longer real-data streams for topology-based backends, and
controlled dynamic stochastic block model (DSBM) stress streams. The results
show that ComNetX can preserve the quality of strong modularity-based solvers
while reducing update time on large graphs: in paired runs on the largest real
graph, Local Leiden keeps final modularity within 0.005 of full-snapshot
recomputation while achieving a $47.0\pm8.1\times$ speedup. The combined
protocols also identify regimes where locality breaks down and a full refresh
is preferable.
\end{abstract}

\begin{IEEEkeywords}
dynamic graphs, community detection, graph clustering, modularity, local
algorithms, graph neural networks
\end{IEEEkeywords}

%----------------------------------------------------------------------------------------

\section{Introduction}
Dynamic networks model systems whose interactions change over time, including social, biological, communication, and information networks~\cite{N03,OSH07,PBV07}. Community detection (CD) is a central task in such data: it identifies groups of vertices with coherent structural or functional roles and supports monitoring, recommendation, anomaly detection, and exploratory analysis~\cite{POM09,SF09,RCC04,FL02}. Modularity remains one of the most widely used quality criteria~\cite{NG04,BDG08}, and scalable methods such as Louvain and Leiden are strong practical baselines~\cite{BGLL08,TWE19,Sah23}. However, these methods were mainly designed for static snapshots. In dynamic settings, even a small batch of edge updates may require rerunning a solver on the entire graph, which is prohibitive for large networks.

Existing dynamic CD methods reduce this cost by reusing previous partitions, applying incremental modularity updates, or processing affected neighborhoods~\cite{RC17,LHK14,HKT17,ZK21,SLe24}. These ideas are essential for scalability, but they are often tied to a specific objective or update rule. Meanwhile, modern graph clustering increasingly incorporates attributes and neural representations~\cite{CCL23,ZXY20,DSD24,LXZ23,JLL23,TPP23}. Feature-aware and GNN-based methods can capture richer dependencies than purely topological heuristics, yet repeatedly applying them to full snapshots is even more expensive. A dynamic framework should therefore support both classical and feature-aware solvers without redesigning each solver from scratch.
Continuous-time temporal community methods address a related but different
problem: they track communities directly in link streams and model when
vertices enter or leave them. We discuss this line in Section~\ref{S:5}, but
the target of ComNetX is batched snapshot maintenance for large dynamic graphs.

The key challenge is locality. If a batch update modifies only a small part of the graph, the algorithm should inspect and update only a bounded neighborhood of the affected vertices. This is natural for real-time graph analytics, but preserving the quality of a strong base solver under local processing is nontrivial: an overly small subgraph can lose important community context, whereas an overly large one eliminates the computational benefit.

In this paper, we present \textit{ComNetX}, a local hierarchical framework for dynamic community detection. ComNetX maintains a hierarchy of communities, identifies the communities affected by a batch update, constructs compact aggregated subproblems, and invokes the selected solver only on these local instances. The aggregation step preserves surrounding community context while avoiding full-snapshot recomputation. When a solver uses node attributes, the same aggregation pattern is applied to the feature matrix, making the framework compatible with topology-only and feature-aware methods. In this way, ComNetX acts as an algorithmic layer around existing CD solvers rather than as a solver tied to a single objective.

Our contributions are:
\begin{enumerate}[leftmargin=*]
    \item We formalize a unified dynamic adaptation setting for arbitrary community detection solvers and contrast naive full-snapshot recomputation with local processing.
    \item We introduce ComNetX, a hierarchical local wrapper that aggregates affected communities, transfers features when required, and projects updated labels back to the original graph.
    \item We analyze the update cost in terms of the affected neighborhood, the touched communities, and the contracted backend instances.
    \item We evaluate the framework with a layered protocol: a broad
    multi-backend compatibility study, longer real-data streams, and controlled
    dynamic stochastic block model (DSBM) stress streams that separate random,
    hub-centered, and
    community-internal updates.
\end{enumerate}

The paper is organized as follows. Section~\ref{S:2} gives the preliminary definitions. Section~\ref{S:3} presents the ComNetX methodology and analysis. Section~\ref{S:4} reports the experiments. Section~\ref{S:5} reviews related work, and Section~\ref{S:6} concludes the paper.
%----------------------------------------------------------------------------------------

%%%%%%%%%%%%%%%%%%METHOD%%%%%%%%%%%%%%%%%%
\section{Preliminaries}\label{S:2}
\subsection{Dynamic Graph Model}

Let $G_t=(V,E_t,\mathbf{A}_t,\mathbf{X})$ denote a graph snapshot at
discrete time $t$. We assume a fixed vertex set with
$n=|V|$ vertices; isolated vertices may appear or disappear from the edge set.
The sparse weighted adjacency matrix is $\mathbf{A}_t\in\mathbb{R}^{n\times n}$.
When node attributes are available, they are stored in
$\mathbf{X}\in\mathbb{R}^{n\times d}$; otherwise the method can run with
synthetic or no features, depending on the selected backend.

The transition from $G_{t-1}$ to $G_t$ is represented by a sparse signed batch
update $\Delta_t$:
\begin{equation}
    \mathbf{A}_t = \mathbf{A}_{t-1} + \Delta_t .
\end{equation}
A positive entry inserts or increases an edge weight, while a negative entry
deletes or decreases it. The endpoints of non-zero entries form the directly
affected set
\begin{equation}
    S_t = \{\, i \in V \mid \exists j:\Delta_t(i,j)\ne 0
          \ \text{or}\ \Delta_t(j,i)\ne 0 \,\}.
\end{equation}
For directed input graphs, ComNetX uses the symmetrized adjacency only for
detecting affected neighborhoods; the stored adjacency and the downstream
metric computation keep the representation selected by the experiment.

\subsection{Community Detection Interface}

A community detection solver is treated as a black-box procedure
\begin{equation}
    \mathcal{B}(\mathbf{A},\mathbf{X},\mathbf{y}_0) \mapsto \mathbf{y},
\end{equation}
where $\mathbf{A}$ is an adjacency matrix, $\mathbf{X}$ is optional, and
$\mathbf{y}_0$ is an optional warm-start partition. This interface covers
modularity heuristics such as Leiden and FLMIG, feature-aware GNN clustering
models such as DMoN, MAGI, S$^2$CAG, and MFC, and dynamic backends such as
DF-Leiden when they are used as local solvers. A \textit{naive} dynamic
adaptation of $\mathcal{B}$ simply runs it on each full snapshot:
\begin{equation}
    \mathbf{y}_t =
    \mathcal{B}(\mathbf{A}_t,\mathbf{X},\mathbf{y}_{t-1}).
\end{equation}
This is often a strong quality baseline, but its update time depends on the
full graph rather than on the changed region.

\subsection{Modularity}
For topology-based evaluation we use modularity~\cite{NG04}. On an undirected
weighted graph with total edge weight $m$ and partition labels $c_i$, it is
\begin{equation}
Q = \frac{1}{2m} \sum_{i,j}
\left[A_{ij} - \gamma\frac{d_i d_j}{2m}\right]
\mathbf{1}\{c_i=c_j\},
\end{equation}
where $d_i=\sum_j A_{ij}$ and $\gamma$ is the resolution parameter. We use
$\gamma=1$ in all reported experiments. Modularity is not a ground-truth
measure and is affected by known resolution limits~\cite{G10,FH16}; therefore
we also report NMI as a compact external-agreement measure whenever labels are
available.

\section{Methodology}\label{S:3}
\subsection{Overview}

ComNetX is a local adaptation layer around a base solver $\mathcal{B}$. It does
not change the objective optimized by $\mathcal{B}$. Instead, after each batch
update it constructs a compact local instance that preserves the community
context around affected vertices. The maintained state consists of
\begin{itemize}[leftmargin=*]
    \item the accumulated sparse adjacency matrix $\mathbf{A}_t$;
    \item the feature matrix $\mathbf{X}$ when the backend requires features;
    \item a hierarchy $\mathbf{C}_t\in\mathbb{Z}^{L\times n}$, where
    $\mathbf{C}_t[\ell,i]$ is the community label of vertex $i$ at hierarchy
    level $\ell$.
\end{itemize}
Figure~\ref{fig:method} summarizes the update workflow. A new batch is first
added to the accumulated graph, its endpoints are expanded to a small graph
neighborhood, and then ComNetX reoptimizes only the communities intersecting
that region. At each level, these communities are contracted into supernodes,
the base solver is executed on the contracted graph, and the result is mapped
back to the original vertices.

\begin{figure*}[t]
    \centering
    \includegraphics[width=.74\textwidth,trim=0 45 0 45,clip]{method.jpg}
    \caption{ComNetX update workflow: affected vertices are expanded, affected
    communities are contracted into local subproblems, the selected backend is
    run locally, and updated labels are written back to the hierarchy.}
    \label{fig:method}
\end{figure*}

\subsection{Affected Region and Hierarchical Closure}

Let $r$ be a small integer radius. ComNetX expands the directly affected set
$S_t$ to
\begin{equation}
B_r(S_t)=\{v\in V \mid \operatorname{dist}_{\mathbf{A}_t}(v,S_t)\le r\}.
\end{equation}
This expansion is performed by repeated sparse
matrix--vector multiplication on the symmetrized adjacency, which makes the
same routine applicable to directed and undirected inputs.

The graph ball alone is not sufficient for high-quality local recomputation:
if only part of an existing community is included, the local solver loses the
context needed to decide whether that community should split, merge, or remain
unchanged. Therefore, at each hierarchy level $\ell$ we close the affected set
under the current community labels:
\begin{equation}
U_t^\ell =
\{v\in V \mid \mathbf{C}_{t-1}[\ell,v]\in
\mathbf{C}_{t-1}[\ell,B_r(S_t)]\}.
\end{equation}
Only vertices in $U_t^\ell$ may be relabeled at level $\ell$; vertices outside
this set keep their previous labels. This is the main difference between
ComNetX and a pure radius-limited update: the radius controls where the update
starts, while the hierarchy controls how much community context is preserved.

\subsection{Local Aggregation}

For a fixed level $\ell$, let $U=U_t^\ell$ and let
$g:U\rightarrow\{1,\ldots,k_\ell\}$ map each vertex to the compact identifier
of its current community at that level. ComNetX builds a sparse membership
matrix $\mathbf{P}_\ell\in\{0,1\}^{k_\ell\times n}$:
\begin{equation}
    \mathbf{P}_\ell[a,i]=1
    \quad\Longleftrightarrow\quad i\in U\ \text{and}\ g(i)=a .
\end{equation}
The local contracted graph is then
\begin{equation}
    \bar{\mathbf{A}}_\ell
    = \mathbf{P}_\ell \mathbf{A}^{(\ell)}_t \mathbf{P}_\ell^\top ,
\end{equation}
where $\mathbf{A}^{(\ell)}_t$ is the working adjacency at the current level.
Thus edge weights between two supernodes are exactly the sums of edge weights
between the corresponding communities.

If the selected backend uses features, ComNetX constructs supernode features by
summing or averaging member features:
\begin{equation}
    \bar{\mathbf{X}}_\ell[a]
    =
    \begin{cases}
    \sum_{i:g(i)=a}\mathbf{X}_i, & \text{sum mode},\\[2mm]
    |g^{-1}(a)|^{-1}\sum_{i:g(i)=a}\mathbf{X}_i,
    & \text{normalized mode}.
    \end{cases}
\end{equation}
The experiments use normalized aggregation for feature-aware local runs. For
topology-only solvers, feature aggregation is skipped.

\subsection{Relabeling and Hierarchy Update}

The base solver is invoked on the aggregated instance:
\begin{equation}
    \bar{\mathbf{y}}_\ell =
    \mathcal{B}(\bar{\mathbf{A}}_\ell,\bar{\mathbf{X}}_\ell,\varnothing).
\end{equation}
The aggregated labels are projected back to the original graph by assigning all
vertices represented by the same supernode to the corresponding new local
cluster. ComNetX reuses existing community identifiers whenever
possible, which avoids unnecessary relabeling of unchanged regions.

After updating level $\ell$, ComNetX removes from the working adjacency all
edges whose endpoints now lie in different communities at that level. This
``cut'' operation ensures that the next hierarchy level refines only the
interior of the communities produced so far. Before the level loop begins,
vertices in the expanded affected set are opened as singletons at the deepest
stored level, and affected higher-level communities inherit the finer labels.
This gives the backend enough freedom to split a changed region while keeping
the unaffected part of the hierarchy fixed. Algorithm~\ref{alg:comnetx}
summarizes the update procedure for one batch.

\begin{algorithm}[t]
\caption{ComNetX update for one batch}
\label{alg:comnetx}
\begin{algorithmic}[1]
\Require accumulated graph $\mathbf{A}_{t-1}$, hierarchy $\mathbf{C}_{t-1}$,
features $\mathbf{X}$, update $\Delta_t$, radius $r$, backend $\mathcal{B}$
\State $\mathbf{A}_t \gets \mathbf{A}_{t-1}+\Delta_t$
\State $S_t \gets$ endpoints of nonzero entries in $\Delta_t$
\State $B \gets B_r(S_t)$
\State open vertices in $B$ as singletons at the deepest hierarchy level
\State propagate finer labels inside affected higher-level communities
\For{each hierarchy level $\ell$}
    \State $U^\ell_t \gets$ vertices in communities intersecting $B$
    \State build $\mathbf{P}_\ell$ from labels $\mathbf{C}_{t-1}[\ell,U^\ell_t]$
    \State $\bar{\mathbf{A}}_\ell \gets
        \mathbf{P}_\ell \mathbf{A}^{(\ell)}_t \mathbf{P}_\ell^\top$
    \State aggregate $\bar{\mathbf{X}}_\ell$ if $\mathcal{B}$ requires features
    \State $\bar{\mathbf{y}}_\ell \gets
        \mathcal{B}(\bar{\mathbf{A}}_\ell,\bar{\mathbf{X}}_\ell,\varnothing)$
    \State map $\bar{\mathbf{y}}_\ell$ back to vertices in $U^\ell_t$
    \State cut inter-community edges in $\mathbf{A}^{(\ell)}_t$
\EndFor
\State \Return updated hierarchy $\mathbf{C}_t$
\end{algorithmic}
\end{algorithm}

\subsection{Complexity and Locality}

Let $b_t=|B_r(S_t)|$, let $h_\ell=|U_t^\ell|$, and let
$k_\ell$ be the number of affected communities contracted at level $\ell$.
For one update, ComNetX performs neighborhood expansion, sparse aggregation,
and one backend call per level. Its time can be written as
\begin{equation}
\begin{aligned}
O\!\Big(&\operatorname{nnz}(\Delta_t)+\operatorname{expand}_r(S_t)\\
&+\sum_{\ell=0}^{L-1}\big[
\operatorname{nnz}(\mathbf{A}_t[U_t^\ell])
+T_{\mathcal{B}}(k_\ell,\bar m_\ell,\bar d)\big]\Big).
\end{aligned}
\end{equation}
where $\bar m_\ell=\operatorname{nnz}(\bar{\mathbf{A}}_\ell)$ and $\bar d$ is
the aggregated feature dimension when features are used. The memory footprint
of a backend call is governed by the contracted local graph, not by the full
snapshot.

In the worst case a high-degree update or a giant affected community can make
$U_t^\ell$ comparable to $V$, and ComNetX degenerates toward full
recomputation. This limitation is unavoidable for local graph algorithms that
preserve community context. The empirical question is therefore whether
$U_t^\ell$ remains small for realistic update batches. The experiments below
use $r=1$ and $L=3$, a configuration selected because larger radii quickly
increase the inspected region on the studied graphs while depth values below
three reduce the benefit of the hierarchy.


%----------------------------------------------------------------------------------------

\section{Experiments} \label{S:4}

This section evaluates ComNetX as a local adapter for static and dynamic
community detection solvers. We compare it with full-snapshot recomputation
and with native dynamic baselines, focusing on the quality--time trade-off
after a sequence of graph updates.
The experiments are organized around five questions:
\begin{itemize}[leftmargin=*]
    \item \textbf{RQ1:} Does ComNetX preserve community quality while reducing
    update time relative to full-snapshot recomputation?
    \item \textbf{RQ2:} Can the same local adaptation improve or complement
    native dynamic community detection algorithms?
    \item \textbf{RQ3:} Are the observed speedups explained by genuinely small
    local and contracted backend instances?
    \item \textbf{RQ4:} Which components--radius expansion, hierarchy depth,
    feature aggregation, closure, and contraction--control the
    quality--efficiency trade-off?
    \item \textbf{RQ5:} How stable is the method under different update streams,
    longer horizons, and adversarially large affected regions?
\end{itemize}

\subsection{Experimental setup}

\subsubsection{Datasets and Evaluation Protocols}
We evaluate on six real-world networks summarised in Table~\ref{tab:datasets}.
The selected datasets provide ground-truth labels, which allows us to complement
modularity with external clustering metrics. For this study all graphs are
converted to an undirected representation by symmetrizing the adjacency matrix.
This keeps the input format uniform across baselines, including methods whose
original implementations do not support directed updates.

The evaluation is deliberately split into three protocols, because a single
short update slice is useful for broad baseline coverage but is not sufficient
as standalone evidence for dynamic graph mining.

\textbf{P1: multi-backend compatibility study.}
For each real network, the edge sequence is partitioned into $1000$ coarse
batches. The first $999$ batches form $G_{999}$, a reference partition
$P(999)$ is computed with Leiden~\cite{TWE19}, and the final coarse batch is
split into ten mini-batches. This short $999{:}10$ protocol is used for broad
backend coverage, including expensive GNN baselines, but not as standalone
evidence of long-horizon stability.

\textbf{P2: real-data batch-sensitivity and long-horizon runs.}
A strategy $n{:}m$ uses the first $0.n$ share of the chronological edge stream
as the initial graph and splits the rest into $m$ update batches. We use
$n\in\{9,99,999\}$ and horizons up to $500$ updates. This is the main protocol
for dynamic stability because it varies both initial history and update
granularity.

\textbf{P3: controlled DSBM stress streams.}
The synthetic protocol uses a dynamic stochastic block model (DSBM) construction
based on the stochastic block model (SBM), a standard generative model for
graphs with block-dependent edge probabilities~\cite{HLL83}. In the dynamic
setting, the graph is observed as a sequence of snapshots generated under a
time-indexed block structure~\cite{XH14}. We instantiate this idea with an
initial planted-partition graph followed by controlled update layers at $5$,
$10$, and $100$ batch granularities. These streams vary update size and update
locality explicitly rather than reusing the real-data $999{:}10$ protocol.

\begin{table}[ht]
\centering
\caption{Real-world datasets.}
\label{tab:datasets}
\begin{tabular}{lrr}
\hline
\textbf{Dataset} & \textbf{Nodes} & \textbf{Edges} \\
\hline
dyn\_cora      & 2\,708   & 5\,278    \\
dyn\_acm       & 3\,025   & 13\,128   \\
dyn\_citeseer  & 3\,327   & 4\,552    \\
patent         & 12\,214  & 41\,916   \\
dyn\_pubmed    & 19\,717  & 44\,338   \\
arxivmath      & 270\,013 & 799\,745  \\
\hline
\end{tabular}
\end{table}

All wall-clock measurements were collected on a Dockerized Linux server with
two AMD EPYC 7742 64-core CPUs, 2.0 TiB RAM, and eight NVIDIA A100-SXM4 GPUs
(80 GB each). The NVIDIA driver was 535.216.03, \texttt{nvidia-smi} reported
CUDA 12.2, and the container used Python 3.10.13, PyTorch 2.3.1 with CUDA 11.8,
and TensorFlow 2.14.0.

\subsubsection{Metrics}
We evaluate both community quality and computational efficiency.
\begin{itemize}
    \item \textbf{Modularity} ($Q$): the standard modularity of the partition obtained after processing the update sequence, measured on the final graph of the corresponding protocol.
    \item \textbf{Normalized Mutual Information (NMI)}: an external clustering metric computed against the ground-truth labels and reported as the compact agreement measure in the main tables.
    \item \textbf{Total processing time} ($T$): the cumulative wall-clock time (in seconds) required to process all measured update batches, i.e., the sum of the execution times of the dynamic algorithm over the whole update sequence.
    \item \textbf{Local workload}: the number of expanded vertices $|B_r(S_t)|$, the number of vertices included after community closure $|U_t^\ell|$, and the number of nodes and edges in the contracted backend instances. These values quantify how much of the full graph is actually inspected.
    \item \textbf{Peak memory}: maximum resident CPU memory and, for GNN-based methods, allocated GPU memory.
\end{itemize}
We exclude data-format conversion time; each baseline operates on its native
graph representation. Whenever repeated measurements are available, comparisons
are paired by dataset, directedness, batch strategy, initial history, and
completed update count. The compatibility table is breadth-oriented and uses
audited finite runs under the common P1 stream, while the robustness table
reports the subset with at least five paired repetitions.

\subsubsection{Baselines}
We compare against static full-snapshot solvers, static graph-clustering
models adapted through the same snapshot interface, and native dynamic
baselines. All methods are evaluated on the same symmetrized graph sequence for
each dataset. \textit{Leidenalg}~\cite{TWE19} is the main topology-only static
baseline and represents strong modularity optimization under full
recomputation. \textit{FLMIG}~\cite{TTB24} is included as a Louvain-style local
move method with aggressive pruning. For representation-learning and
feature-aware graph clustering, we include DMoN~\cite{TPP23},
MAGI~\cite{LLC24}, S$^2$CAG~\cite{LYZ25}, and the PRGPT-Infomap and
PRGPT-Locale variants~\cite{QZG24}; DMoN, MAGI, and S$^2$CAG use node
features when available, whereas the PRGPT variants are evaluated through their
topological clustering outputs. The native dynamic baselines are
DF-Leiden~\cite{SLe24}, an OpenMP-parallel dynamic Leiden implementation, and
MFC~\cite{KZL24}, a feature-aware GNN-based dynamic community detection method.
Thus, Leidenalg, FLMIG, PRGPT-Infomap, PRGPT-Locale, and DF-Leiden are treated
as topology-only baselines in our evaluation, while DMoN, MAGI, S$^2$CAG, and
MFC are feature-aware baselines.

For algorithms that involve iterative training, the configured budgets are
$100$ epochs for MFC, one iteration for FLMIG, and $10$ epochs for S$^2$CAG,
MAGI, and DMoN. These values match the experimental configuration used for the
reported runs. MAGI and DMoN are not given ground-truth cluster counts. For
MAGI, after learning node embeddings, we select the number of clusters with a
vMF-mixture elbow criterion and then apply $k$-means; when a previous partition
is available, its number of labels is used only as a warm-start estimate. For
DMoN, whose output dimension is fixed in advance, we set this dimension to the
number of nodes in the current graph and compact the nonempty argmax clusters
after inference.

\textbf{Focused comparison design.}
The evaluation separates breadth from focused evidence. The compatibility
study includes all six real datasets and all implemented baselines with
comparable outputs. The focused tables then use complete two-dataset blocks on
\textit{dyn\_pubmed} and \textit{arxivmath} whenever the goal is a direct
method comparison. Leidenalg represents the strongest topology-only
full-recomputation baseline, DF-Leiden represents a native dynamic topology
solver, and S$^2$CAG represents modern feature-aware graph clustering under
memory and runtime pressure. FLMIG, PRGPT, MAGI, DMoN, and MFC are reported in
the breadth study to document compatibility, sensitivity, and feasibility across
a wider method family; they are not repeated as singleton rows in focused
tables unless a complete, table-specific comparison is available.
Synthetic DSBM streams are used only for operating-envelope stress tests and are
not mixed with real-data quality tables. Continuous-time temporal-community
methods are discussed in Related Work as complementary rather than treated as
like-for-like baselines for batched snapshot maintenance.

\subsubsection{Configuration of the proposed local adaptation}
Our dynamic adapter uses three hierarchy levels and a graph-neighborhood radius
of one, meaning that the endpoints of the update are expanded to their
immediate graph neighbors before community closure is applied. Separate
neighborhood-size measurements show that larger radii can quickly approach the
full graph on several datasets; this motivates the radius-one default.

For feature-aware local runs, the configuration supplies random
$64$-dimensional features and uses normalized feature aggregation. Naive runs
use the dataset-provided features. This means that the GNN comparisons test the
complete local deployment recipe rather than isolating locality alone; a
separate feature-mode ablation is needed to attribute the gains precisely.

\subsection{Experimental results}

\subsubsection{Compatibility Study Across Backends}

This experiment addresses RQ1.
Table~\ref{tab:main-results} summarizes the modularity--runtime trade-off and
NMI agreement with ground-truth communities across all datasets under protocol
P1. It is a breadth check over heterogeneous backends under the same update
interface, so we report the aggregate quality--efficiency pattern
rather than every raw per-dataset cell. The reported
$\overline{\Delta Q}$ and $\overline{\Delta\mathrm{NMI}}$ values are Local
minus baseline, averaged over datasets with finite baseline results. Geometric
mean speedups exclude zero-time and out-of-memory baseline entries.
For each method we contrast the conventional dynamic baseline (``Naive'' for static methods that re-run on the full snapshot, ``Dynamic'' for natively dynamic algorithms) with the variant obtained through our local hierarchical adapter (``Local'').
The Local variant is also applied to the natively dynamic algorithms DF-Leiden
and MFC. In this setting, the backend receives the current local graph, the
corresponding initial partition, and the current update batch, but its execution
is restricted to the subgraph induced by the batch neighbourhood and the
corresponding sub-communities of the hierarchical decomposition. The results
show that this local restriction can complement native dynamic solvers on large
graphs, although the benefit is backend- and dataset-dependent. The
long-horizon and stress-test protocols below then test whether the same
locality mechanism remains stable as the stream becomes longer or the affected
region becomes less local.

\begin{table*}[ht]
\centering
\caption{Compatibility study across backends.}
\label{tab:main-results}
\setlength{\tabcolsep}{4pt}
\footnotesize
\begin{tabular}{llrrrrr}
\toprule
\textbf{Backend} & \textbf{Baseline} & \textbf{$\overline{\Delta Q}$} &
\textbf{$\overline{\Delta\mathrm{NMI}}$} & \textbf{Geo. speedup} &
\textbf{arxivmath} & \textbf{OOM avoided} \\
\midrule
DF-Leiden      & Dynamic & $-$0.012 & +0.002 & 0.6$\times$  & 8.5$\times$ & 0 \\
MFC            & Dynamic & +0.048 & +0.126 & 45.1$\times$ & OOM$\rightarrow$381.7s & 1 \\
Leidenalg      & Naive   & $-$0.013 & $-$0.010 & 7.3$\times$  & 41.4$\times$ & 0 \\
FLMIG          & Naive   & $-$0.250 & $-$0.042 & 60.0$\times$ & 2731.8$\times$ & 0 \\
PRGPT-Infomap  & Naive   & $-$0.165 & $-$0.073 & 7.6$\times$  & 39.8$\times$ & 0 \\
PRGPT-Locale   & Naive   & $-$0.242 & $-$0.073 & 8.9$\times$  & 58.9$\times$ & 0 \\
S$^2$CAG       & Naive   & +0.230 & +0.112 & 8.7$\times$  & 12.8$\times$ & 0 \\
MAGI           & Naive   & +0.082 & $-$0.034 & 0.3$\times$  & OOM$\rightarrow$868.2s & 1 \\
DMoN           & Naive   & +0.484 & +0.112 & 1.4$\times$  & OOM$\rightarrow$65.1s & 1 \\
\bottomrule
\end{tabular}
\end{table*}

\textbf{Modularity and external agreement.}
For Leidenalg, Table~\ref{tab:main-results} shows that the Local adapter
closely tracks full-snapshot recomputation, with mean changes of $-0.013$ in
modularity and $-0.010$ in NMI.
FLMIG, whose aggressive pruning strategy is more sensitive to the restriction
to a local neighbourhood, obtains large speedups but loses more modularity and
NMI.
The GNN-based methods show a consistent pattern: in the Naive (full-graph)
setting, S$^2$CAG, DMoN, and MAGI are often weaker or infeasible under the
same update protocol.
When switched to the Local mode with compact random features, S$^2$CAG and
DMoN obtain higher mean modularity and NMI; MAGI mainly benefits through
making the largest graph executable, with a smaller aggregate quality gain.
Because locality and feature mode change together in these GNN runs, the
result should be interpreted as evidence that the complete ComNetX deployment
can make such models feasible on local updates; it does not by itself isolate
the contribution of random features.
The PRGPT variants are less robust to the local restriction: their aggregate
modularity and NMI deltas are negative despite consistent speedups, indicating
that these methods rely more heavily on the global context provided by the full
graph.
The Local adapter also complements native dynamic solvers. For DF-Leiden the
mean NMI change is nearly neutral and the large-graph speedup is substantial;
for MFC, the Local version avoids the out-of-memory failure on the largest
graph and improves the aggregate quality measures in
Table~\ref{tab:main-results}, although its absolute quality remains below that
of the best topology-based methods.

\textbf{Reduction in processing time.}
The runtime advantage of the Local adapter is substantial for the larger
full-recomputation baselines.
For Leidenalg, Table~\ref{tab:main-results} reports a $41.4\times$ speedup on
\textit{arxivmath}, and the paired robustness table reports
$22.7\pm0.8\times$ on \textit{dyn\_pubmed} and $47.0\pm8.1\times$ on
\textit{arxivmath}. FLMIG and the PRGPT variants obtain even larger or
consistent large-graph speedups, but their quality deltas show that this speed
comes from a more aggressive loss of global context. The GNN rows show a
different benefit: S$^2$CAG becomes substantially faster, while DMoN and MAGI
become executable on large graphs because only small neighbourhood graphs are
materialised on the GPU. The DF-Leiden row shows that locality can also help an
algorithm already designed for dynamic updates when the graph is large enough.

\subsubsection{Statistical Robustness}

This experiment addresses the stream-level part of RQ5 without mixing in the
random-feature runs. Table~\ref{tab:stability} reports repeated non-random
measurements where Local and baseline methods share the same dataset,
directedness, batch strategy, and completed update count. Each row fixes the
algorithm parameters; the standard deviations therefore reflect repeated
measurements of the same stream shape rather than a mixture of different
horizons or feature draws. The Local rows use the fixed three-level, radius-one
configuration and are compared with the corresponding naive or native dynamic
baseline. Table~\ref{tab:stability} keeps only the \texttt{999:10} rows for
\textit{dyn\_pubmed} and \textit{arxivmath} with at least five repeated
measurements in the audited result set. Values are reported as mean $\pm$
standard deviation. $T$ denotes cumulative processing time in seconds, and B/L
summarizes the observed baseline-to-Local runtime ratio.

\begin{table*}[t]
\centering
\caption{Statistical robustness over repeated 999:10 runs.}
\label{tab:stability}
\setlength{\tabcolsep}{3pt}
\scriptsize
\begin{tabular}{@{}lllrccccc@{}}
\toprule
\textbf{Backend} & \textbf{Dataset} & \textbf{Stream} & \textbf{$n$} &
\textbf{$Q_L$} & \textbf{$Q_B$} & \textbf{$T_L$} &
\textbf{$T_B$} & \textbf{B/L} \\
\midrule
\multicolumn{9}{l}{\textit{Leiden, Local versus full recomputation}} \\
Leiden & dyn\_pubmed & 999:10 & 6 & 0.776 $\pm$ 0.000 & 0.786 $\pm$ 0.000 & 0.39 $\pm$ 0.15 & 9.02 $\pm$ 3.69 & 22.7 $\pm$ 0.8 \\
Leiden & arxivmath   & 999:10 & 6 & 0.881 $\pm$ 0.000 & 0.886 $\pm$ 0.000 & 4.00 $\pm$ 0.53 & 187.52 $\pm$ 37.06 & 47.0 $\pm$ 8.1 \\
\addlinespace
\multicolumn{9}{l}{\textit{DF-Leiden, Local versus native dynamic backend}} \\
DF-Leiden & dyn\_pubmed & 999:10 & 7 & 0.779 $\pm$ 0.000 & 0.762 $\pm$ 0.000 & 0.15 $\pm$ 0.07 & 0.11 $\pm$ 0.01 & 0.8 $\pm$ 0.3 \\
DF-Leiden & arxivmath   & 999:10 & 6 & 0.880 $\pm$ 0.000 & 0.850 $\pm$ 0.000 & 0.50 $\pm$ 0.05 & 4.23 $\pm$ 0.56 & 8.5 $\pm$ 0.5 \\
\addlinespace
\multicolumn{9}{l}{\textit{S$^2$CAG, dataset features}} \\
S$^2$CAG & dyn\_pubmed & 999:10 & 5 & 0.530 $\pm$ 0.000 & 0.622 $\pm$ 0.000 & 91.88 $\pm$ 45.40 & 191.26 $\pm$ 49.43 & 2.4 $\pm$ 0.8 \\
S$^2$CAG & arxivmath   & 999:10 & 5 & 0.089 $\pm$ 0.000 & 0.001 $\pm$ 0.000 & 227.39 $\pm$ 12.35 & 2972.22 $\pm$ 418.59 & 13.1 $\pm$ 1.6 \\
\bottomrule
\end{tabular}
\end{table*}

This repeated-run view is intentionally narrower than a whole-study aggregate.
It shows that the topology-only conclusions are stable when the stream shape is
held fixed: Leiden keeps a small modularity gap and large speedups, while Local
DF-Leiden is useful on \textit{arxivmath} but not on \textit{dyn\_pubmed},
where the native dynamic backend is already efficient. The S$^2$CAG
dataset-feature rows confirm that the Local wrapper can reduce runtime for this
heavier backend, but with a larger quality gap and with less consistent speedup
on the denser \textit{dyn\_pubmed} streams. The remaining runtime standard
deviation is mostly run-to-run system variation and backend cost variation
under the same deterministic setting, not a random-feature effect. We keep this
table focused on modularity and runtime; NMI is reported in the compatibility
and long-horizon summaries.

\subsubsection{Comparison with Native Dynamic Methods}

This experiment addresses RQ2. We compare the best adapted static algorithms
against native dynamic solvers DF-Leiden and MFC in the compatibility and
robustness studies above. This is the main like-for-like comparison because all
methods are evaluated on the same batched snapshot stream and the same
final-snapshot modularity and NMI summaries. The comparison is deliberately
framed as an operating-envelope result rather than a universal dominance claim.
In the paired \texttt{999:10} rows, Local DF-Leiden improves modularity on both
\textit{dyn\_pubmed} and \textit{arxivmath}, while
Table~\ref{tab:main-results} shows a nearly neutral aggregate NMI change for
DF-Leiden. At the 500-update horizon, Table~\ref{tab:long-horizon} is more
conservative: Local DF-Leiden remains faster, but loses final NMI on both large
datasets.

DF-Leiden is already highly optimized, so the Local wrapper is not always the
fastest choice. On \textit{dyn\_pubmed} the paired \texttt{999:10} rows show
that the Local wrapper is slower than native DF-Leiden, whereas on
\textit{arxivmath} it yields an $8.5\pm0.5\times$ speedup; the same large-graph
pattern persists at the 500-update horizon, where Local DF-Leiden is
$7.4\times$ faster. MFC remains substantially slower and runs out of memory on
the largest dataset under the full dynamic setting. Thus, native dynamic
solvers remain important speed baselines, while ComNetX is most useful when the
affected region stays small and the backend would otherwise process a much
larger snapshot.

\subsubsection{Locality and Workload Reduction}

This experiment addresses RQ3.
The main mechanism behind the speedup is that ComNetX transforms a full-snapshot
backend call into a sequence of small local calls. Table~\ref{tab:workload}
reports the measured growth of update neighborhoods in the real-data streams.
Since ComNetX uses $r=1$ in the main configuration, the most important column
is $|B_1(S_t)|/|V|$; the larger radii show how quickly locality would be lost
if the expansion radius were increased.
Here $|S_t|$ is the average number of directly affected vertices. The
$B_h$ columns report the mean percentage of vertices within graph distance $h$
from $S_t$; for $B_1$, the table also shows the maximum mini-batch percentage.

\begin{table}[H]
\centering
\caption{Empirical neighborhood growth.}
\label{tab:workload}
\setlength{\tabcolsep}{2pt}
\scriptsize
\begin{tabular}{@{}lrrrrrr@{}}
\toprule
\textbf{Dataset} & \textbf{Upd.} & \textbf{$|S_t|$} &
\textbf{$B_1$} & \textbf{$B_2$} & \textbf{$B_3$} & \textbf{$B_5$} \\
\midrule
dyn\_cora     & 10 & 2.0   & 0.35 / 0.41 & 1.75 & 7.77  & 48.63 \\
dyn\_acm      & 10 & 5.2   & 2.04 / 3.11 & 8.11 & 21.61 & 54.40 \\
dyn\_citeseer & 8  & 2.0   & 1.01 / 3.13 & 3.06 & 5.82  & 15.58 \\
patent        & 10 & 5.1   & 4.49 / 4.58 & 5.46 & 18.57 & 33.27 \\
dyn\_pubmed   & 10 & 8.8   & 0.79 / 1.80 & 7.28 & 30.58 & 86.95 \\
arxivmath     & 10 & 145.6 & 0.95 / 1.11 & 7.01 & 27.36 & 76.84 \\
\bottomrule
\end{tabular}
\end{table}

The measured neighborhoods support the choice $r=1$: the average inspected
radius-one region is below $5\%$ of the graph for all six datasets and below
$1\%$ on the two largest citation graphs, \textit{dyn\_pubmed} and
\textit{arxivmath}. At the same time, the larger-radius columns show that the
locality budget is fragile. Moving from $r=1$ to $r=3$ would already inspect
about $27$--$31\%$ of the vertices on \textit{arxivmath} and
\textit{dyn\_pubmed}, and radius five would cover most of these graphs. This
explains why the default configuration uses immediate neighborhoods and relies
on hierarchical community closure, rather than deeper graph-radius expansion,
to preserve context.

The neighborhood table explains the first stage of locality. The decisive
second stage is the hierarchical closure and contraction, summarized by the
post-closure sets $U_t^\ell$ and the contracted backend sizes
$(k_\ell,\bar m_\ell)$. Figure~\ref{fig:workload-speedup} uses the focused
workload grid and adds \textit{dyn\_cora} as a small-graph control. The
x-axis reports the contracted edge fraction $|\bar E|/|E|$, while the y-axis
reports the corresponding Base/Local time ratio. Marker area encodes the
post-closure vertex fraction $|U|/|V|$, and the labels C, P, and A denote
\textit{dyn\_cora}, \textit{dyn\_pubmed}, and \textit{arxivmath}. The backend
receives sub-percent or near-percent edge fractions even when post-closure
sets are substantially larger; the resulting speedups remain backend-dependent,
because DF-Leiden already performs local dynamic updates and S$^2$CAG pays
feature-materialization overhead.

Profiled timing on the focused \texttt{999:50} streams separates algorithmic
locality from implementation overhead. Radius expansion, closure, aggregation,
and projection stay in the millisecond range on \textit{dyn\_pubmed} and
\textit{arxivmath}; backend execution is the dominant term for Leiden on
\textit{arxivmath} and for all S$^2$CAG rows. Thus, the speedups in
Figure~\ref{fig:workload-speedup} are driven by smaller contracted backend
instances, while the local-maintenance overhead remains explicit and
measurable.

\begin{figure}[!b]
\centering
\includegraphics[width=.78\columnwidth]{workload_speedup.pdf}
\caption{Contracted workload and speedup.}
\label{fig:workload-speedup}
\end{figure}

\subsubsection{Ablation Study}

This experiment addresses RQ4.
The ablation suite isolates the tunable choices evaluated in this study:
graph-radius expansion, hierarchy depth, feature
mode, feature aggregation, affected-community closure, and contraction. Radius
and depth are measured with Leiden to isolate the topology-only part of
ComNetX, while feature and aggregation choices are measured with S$^2$CAG.
The closure and contraction mechanism is first quantified through
Table~\ref{tab:workload} and Figure~\ref{fig:workload-speedup}, and then tested
directly in Table~\ref{tab:closure-ablation}.
For the topology ablation, Figure~\ref{fig:topology-ablation} plots the
complete $L\times r$ grid measured for Leiden on the two larger
\texttt{999:10} streams.
Each point gives final modularity $Q$ versus speedup over full-snapshot Leiden;
higher and farther right is better.

\begin{figure}[H]
\centering
\includegraphics[width=\columnwidth]{topology_ablation_pareto.pdf}
\caption{Quality--time frontier for the Leiden topology ablation. Marker style
encodes the graph radius $r$, the marker number is the hierarchy depth $L$, and
the black line marks nondominated points.}
\label{fig:topology-ablation}
\end{figure}

Figure~\ref{fig:topology-ablation} shows that the topology parameters are not
monotone quality knobs. On \textit{dyn\_pubmed}, the best local modularity in
the grid is the shallow endpoint-only setting $L=2,r=0$ ($Q=0.783$,
$92.1\times$), while $L=1,r=0$ is faster ($146.4\times$) with only a small
additional quality loss; the deeper $L=4,r=1$ setting drops to $Q=0.736$. On
\textit{arxivmath}, the $r\ge1$ rows cluster near $Q=0.881$, but the runtime
cost grows quickly as the radius increases. Thus the default $L=3,r=1$ is not
selected as the best point of this short ablation; it is the fixed
configuration used across the broader compatibility, workload, and long-horizon
evidence. Radius and hierarchy are workload-dependent locality--context
controls, not parameters that should be claimed optimal for every graph.

For the S$^2$CAG feature-mode ablation on the \texttt{999:100} streams, each
entry is reported as final modularity $Q$, cumulative time $T$, and
final-snapshot NMI.

\begin{table}[H]
\centering
\caption{S$^2$CAG feature-mode ablation.}
\label{tab:feature-ablation}
\setlength{\tabcolsep}{2pt}
\scriptsize
\begin{tabular}{@{}lcc@{}}
\toprule
\textbf{Variant} & \textbf{dyn\_cora} & \textbf{dyn\_pubmed} \\
\midrule
Naive, dataset features & 0.691 / 37.87 / 0.437 & 0.624 / 394.13 / 0.202 \\
Naive, random features  & $-$0.001 / 14.64 / 0.023 & $-$0.002 / 97.50 / 0.000 \\
Naive, one-hot features & 0.751 / 45.12 / 0.360 & 0.528 / 11041.55 / 0.059 \\
Local, dataset features & 0.788 / 8.20 / 0.463 & 0.532 / 39.02 / 0.180 \\
Local, random features  & 0.784 / 4.86 / 0.462 & 0.534 / 27.61 / 0.186 \\
Local, one-hot features & 0.779 / 10.65 / 0.452 & 0.518 / 203.13 / 0.182 \\
\bottomrule
\end{tabular}
\end{table}

Table~\ref{tab:feature-ablation} gives two important cautions. First, random features are
not a drop-in replacement for attributes: in the naive full-graph run they nearly
collapse S$^2$CAG quality. They become useful only as part of the local
ComNetX deployment, where the contracted instances are much smaller and the
topology dominates the update. Second, one-hot features improve the naive
modularity on \textit{dyn\_cora}, but on \textit{dyn\_pubmed} they are
prohibitively expensive and have low final-snapshot NMI; in the local setting,
they do not improve quality enough to offset their additional cost.
In the closure ablation, ``No closure'' keeps only the radius-expanded
vertices, whereas ``No contraction'' gives the backend the post-closure
induced subgraph without community contraction; $T$ is the total profiled time
in seconds.

The direct ablation separates the two roles of the hierarchy. The
\textit{dyn\_cora} control shows the expected small-graph regime: removing
closure or contraction can reduce wall-clock time when overhead dominates, but
the no-contraction instance is about $18\times$ larger than the full contracted
instance and loses modularity. On the larger graphs, removing closure is fastest
but loses substantial modularity because the backend sees only the radius-one
boundary. Removing contraction preserves or improves quality on
\textit{arxivmath}, but the backend instance grows by two orders of magnitude
and the run becomes substantially slower; contraction is therefore the mechanism
that turns closure from context recovery into a scalable update.

\begin{table}[H]
\centering
\caption{Closure and contraction ablation for Leiden.}
\label{tab:closure-ablation}
\setlength{\tabcolsep}{2pt}
\scriptsize
\begin{tabular}{@{}llrrr@{}}
\toprule
\textbf{Variant} & \textbf{Dataset} & \textbf{$Q$} & \textbf{$T$} &
\textbf{$|\bar V|/|V|$} \\
\midrule
Full ComNetX & dyn\_cora & 0.808 & 0.75 & 0.41\% \\
Full ComNetX & dyn\_pubmed & 0.758 & 2.36 & 0.20\% \\
Full ComNetX & arxivmath & 0.871 & 8.90 & 0.24\% \\
No closure & dyn\_cora & 0.797 & 0.11 & 0.35\% \\
No closure & dyn\_pubmed & 0.563 & 0.82 & 0.18\% \\
No closure & arxivmath & 0.732 & 7.60 & 0.22\% \\
No contraction & dyn\_cora & 0.763 & 0.35 & 7.26\% \\
No contraction & dyn\_pubmed & 0.671 & 16.07 & 8.81\% \\
No contraction & arxivmath & 0.874 & 1520.57 & 32.56\% \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{Real-Data Long-Horizon Robustness}

This experiment addresses the drift component of RQ5.
Protocol P1 is useful for checking many backends under a common setup, but it
is only the breadth study. Protocol P2 carries the dynamic-stability claim by
varying both the initial-history fraction and the number of update batches.
Table~\ref{tab:long-horizon} reports a balanced topology snapshot for
\textit{dyn\_pubmed} and \textit{arxivmath} at the 500-update horizon using
paired long-horizon measurements. Denser $99{:}m$ and $999{:}m$ starts are kept
out of the compact table. The
goal is to show not only a large cumulative speedup, but also the range of
stream granularities where quality drift remains controlled.
Rows report final modularity $Q$, final-snapshot NMI, and cumulative time $T$
in seconds; Local speedups are relative to the corresponding non-local backend.

The long-horizon results strengthen the scalability claim for topology-based
backends: Local Leiden is about $17\times$ faster than full Leiden on
\textit{dyn\_pubmed} and about $29\times$ faster on \textit{arxivmath} at the
500-update horizon. At the same time, the NMI gap grows relative to the
ten-update setting, especially on \textit{dyn\_pubmed}, indicating that
long-horizon drift must be reported rather than summarized by one final number.
Per-update runtime and modularity are available for these streams, while NMI is
available at the final snapshot and is reported in Table~\ref{tab:long-horizon}.

\begin{table}[H]
\centering
\caption{Long-horizon topology endpoints.}
\label{tab:long-horizon}
\setlength{\tabcolsep}{2pt}
\scriptsize
\begin{tabular}{@{}llrrrrr@{}}
\toprule
\textbf{Dataset} & \textbf{Method} & \textbf{Updates} &
\textbf{$Q$} & \textbf{NMI} & \textbf{$T$} & \textbf{Speedup} \\
\midrule
dyn\_pubmed & Leiden & 500 & 0.784 & 0.208 & 427.50 & 1.0$\times$ \\
dyn\_pubmed & Local Leiden & 500 & 0.748 & 0.153 & 25.06 & 17.1$\times$ \\
dyn\_pubmed & DF-Leiden & 500 & 0.762 & 0.195 & 8.06 & 1.0$\times$ \\
dyn\_pubmed & Local DF & 500 & 0.715 & 0.138 & 7.35 & 1.1$\times$ \\
\midrule
arxivmath & Leiden & 500 & 0.887 & 0.427 & 8421.63 & 1.0$\times$ \\
arxivmath & Local Leiden & 500 & 0.874 & 0.355 & 289.27 & 29.1$\times$ \\
arxivmath & DF-Leiden & 500 & 0.855 & 0.388 & 243.29 & 1.0$\times$ \\
arxivmath & Local DF & 500 & 0.869 & 0.335 & 32.99 & 7.4$\times$ \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{Controlled DSBM Stress Test}

This experiment addresses the failure-mode component of RQ5 by varying update
size and touched-vertex type in an independent DSBM stress suite. Each
synthetic sequence starts from an SBM-style planted-partition snapshot and then
applies controlled edge updates to produce dynamic graph snapshots. Each graph
has $n=100{,}000$ vertices, $k=32$ planted communities, target average degree
$58$, $1\%$ degree-$512$ hubs, and assortativity
$p_{\mathrm{in}}=0.97$. We evaluate random, hub-centered, and
community-internal updates over $T\in\{5,10,100\}$ batches, with changed-edge
rates from $0.01\%$ to $1\%$. Across generated streams, the mean affected
vertex ranges are 0.58--43.25\% (random), 0.54--25.99\% (hub-centered), and
0.57--14.77\% (community-internal); the corresponding intra-community
changed-edge shares are about 50\%, 50\%, and 100\%.

The DSBM stress study exposes the operating envelope directly. At the smallest
update size, Local Leiden remains faster in all regimes. The completed
five-seed 100-batch set confirms that this boundary is stable: at
$0.01\%$, random, hub-centered, and community-internal streams give
$3.38$--$3.42\times$, $1.54$--$1.57\times$, and $4.10$--$4.59\times$
speedups, whereas at $0.05\%$ only community-internal streams stay above
break-even across all seeds ($1.13$--$1.19\times$). From $0.1\%$ onward, most local runs are
slower than full recomputation, especially on $0.5\%$--$1.0\%$ random and
hub-centered streams. Quality drift is usually small, but the seed sweep
includes one community-stream loss of $0.089$ NMI, and hub-centered $0.5\%$
changes at the longest available window lose $0.0229$
modularity and $0.088$ NMI. Thus, local hierarchical recomputation is useful
for very small or strongly localized changes, while large or hub-heavy update
batches should trigger a full refresh.

\subsubsection{Limitations}

ComNetX is designed for streams where affected communities remain small
relative to the graph. If an update touches a hub, a very large community, or a
region whose boundary is important for the backend objective, the local instance
may approach the full graph or lose useful context. The experiments also show
that backends benefit unevenly from localization: Leiden is stable, whereas
representation-learning methods are more sensitive to restricted context and
feature representation. We therefore present ComNetX as an adaptive locality
framework, with workload, ablation, and stress-test results defining when a
full refresh is preferable.

\section{Related work} \label{S:5}
\textbf{Static and modularity-based community detection.} Classical CD methods
rely on modularity, information flow, label propagation, or related structural
criteria. Louvain and Leiden are widely used because they combine high
modularity with strong scalability~\cite{BGLL08,TWE19}, while Infomap and
label-propagation methods offer alternative flow- and propagation-based
views~\cite{RB07,SF09,TS23}. The quality and limitations of such partitions
have been extensively studied, including resolution effects, ground-truth
evaluation, and robustness of detected communities~\cite{POM09,YL15,YAT16,FH16}.
Recent static methods continue to improve modularity optimization through
greedy disassembly and local-move strategies~\cite{Rustamaji24,TTB24}, but
direct application to evolving graphs still tends to require full
recomputation.

\textbf{Dynamic and local community detection.} Dynamic CD methods balance
snapshot quality, temporal consistency, and update efficiency~\cite{RC17,CZW23,HKT17}.
Incremental modularity and label-propagation algorithms update selected parts
of the previous solution~\cite{SLX14,MZW20,ZCL19,Seif20,XCS13}, while flow-
and evolution-based approaches track persistent structures over time~\cite{Bov22,Sun22,Mazza23}.
Recent work has also moved toward continuous-time temporal community
objectives, such as longitudinal modularity and the LAGO optimizer for tracking
the times when vertices enter and exit communities~\cite{Brabant25}. This line
is complementary to ComNetX: continuous-time methods avoid temporal
discretization, whereas our setting targets batched snapshot maintenance and
focuses on reusing arbitrary snapshot solvers under local updates.
Other work studies sparse temporal observations, fully dynamic graph routines,
and scalable multicore or distributed variants~\cite{Conrad25,Blaskovic25,HHC22,SLu24,SKB25,Sattar25}.
Locality is a key direction in this line: Delta-Screening and dynamic Leiden
variants process affected regions rather than entire snapshots~\cite{ZK21,SLe24};
DLPA-S performs similarity-based local updates with connectivity
post-processing~\cite{DKS26}; VD-STAR supports dynamic structural clustering
with flexible similarity thresholds~\cite{ZGR25}; and Spider combines seeded
geodesic expansion with modularity-guided refinement~\cite{HK26}. These works
motivate the locality requirement, but they are usually tied to a particular
update rule or objective.

\textbf{Attributed and GNN-based graph clustering.} Attributed and GNN-based
methods extend CD beyond purely topological criteria. Surveys and recent
models show that neural embeddings can capture high-order dependencies and
combine naturally with modularity or clustering losses~\cite{CCL23,LXZ23,TPP23,SGH09,LYD24,ZXZ20}.
Examples include modularity-oriented contrastive learning and neural modularity
maximization~\cite{LLC24,BKH24}, structural-entropy objectives~\cite{ZPS25},
pre-train-and-refine pipelines for scalable partitioning~\cite{QZG24,QZG24HPEC},
and spectral subspace clustering for attributed graphs~\cite{LYZ25}. Recent
models also explore fairness-aware deep community detection~\cite{GPT25}.
Although these methods improve expressiveness, they are often expensive to
rerun after each graph update.

Overall, prior work offers either efficient dynamic routines specialized to a
fixed algorithmic design or expressive static and feature-aware solvers that
are costly in dynamic settings. ComNetX is complementary: it acts as a local
adapter around an arbitrary solver, including static algorithms, GNN-based
methods, and native dynamic solvers, while restricting computation to affected
aggregated subproblems.
%----------------------------------------------------------------------------------------
\section{Conclusion} \label{S:6}
We presented ComNetX, a local hierarchical framework for adapting community
detection solvers to dynamic graphs. Instead of recomputing a partition on the
full graph after every batch update, ComNetX maintains a community hierarchy,
expands the updated edge endpoints to a small neighborhood, contracts affected
communities into local subproblems, and invokes the selected backend only on
those subproblems. The same aggregation mechanism supports topology-only and
feature-aware solvers, which makes the framework complementary to existing
static, GNN-based, and dynamic community detection algorithms.

The experiments show that the approach is most effective when a strong backend
can exploit locality without losing global community context. In particular,
Local Leiden preserves the modularity of full-snapshot recomputation on the
studied graphs while reducing update time substantially on larger networks.
The new ablations show that radius, hierarchy depth, and feature representation
are substantive parameters: they can change both quality and runtime by large
factors. The long-horizon topology runs are the first evidence that large
cumulative speedups can persist over extended streams, although quality drift
must be reported explicitly and cannot be inferred from the short multi-backend
study alone.

Overall, ComNetX is effective not merely because it runs a solver on a smaller
subgraph, but because hierarchical community closure preserves enough context
for local recomputation to match full-snapshot quality in the update regimes
where dynamic graph mining needs it most. The resulting method is
solver-agnostic, compatible with feature-aware backends, and equipped with an
explicit fallback interpretation: as the affected region grows, the local
problem approaches the full snapshot, revealing when a full refresh is the
right choice.
%----------------------------------------------------------------------------------------
\begingroup
\begin{thebibliography}{00}
\fontsize{6.2}{6.35}\selectfont\setlength{\itemsep}{0pt}\setlength{\parsep}{0pt}\setlength{\parskip}{0pt}
\bibitem{N03}
    Newman, M.E.J. (2003). The structure and function of complex networks. \textit{SIAM Review}, 45(2), 167--256.
\bibitem{OSH07}
    Onnela, J., Saram{\"a}ki, J., Hyv{\"o}nen, J. et al. (2007). Structure and tie strengths in mobile communication networks. \textit{Proceedings of the National Academy of Sciences}, 104(18), 7332--7336.
\bibitem{PBV07}
    Palla, G., Barab{\'a}si, A., \& Vicsek, T. (2007). Quantifying social group evolution. \textit{Nature}, 446, 664--667. 
\bibitem{HLL83}
    Holland, P.W., Laskey, K.B., \& Leinhardt, S. (1983). Stochastic blockmodels: First steps. \textit{Social Networks}, 5(2), 109--137.
\bibitem{XH14}
    Xu, K.S., \& Hero, A.O. (2014). Dynamic stochastic blockmodels for time-evolving social networks. \textit{IEEE Journal of Selected Topics in Signal Processing}, 8(4), 552--562.
\bibitem{POM09}
    Porter, M.A., Onnela, J.P., \& Mucha, P.J. et al. (2009). Communities in Networks. \textit{Notices AMS}, 56(9), 1082--1097.
\bibitem{SF09}
    Fortunato, S. (2010). Community detection in graphs. \textit{Physics Reports}, 486(3--5), 75--174. 
\bibitem{RCC04}
    Radicchi, F., Castellano, C., \& Cecconi, F. et al. (2004). Defining and identifying communities in networks. \textit{Proceedings of the National Academy of Sciences}, 101(9).
\bibitem{FL02}
    Flake, G.W., \& Lawrence, S. (2002). Self-Organization and Identification of Web Communities. \textit{Computer}, 35(3), 66--70.
\bibitem{RB07}
    Rosvall, M., \& Bergstrom, C.T. (2007). An information-theoretic framework for resolving community structure in complex networks. \textit{Proceedings of the National Academy of Sciences}, 104, 7327--7331.
\bibitem{TS23}
    Traag, V.A., {\v S}ubelj, L. (2023). Large network community detection by fast label propagation. \textit{Scientific Reports}, 13, 2701.
\bibitem{NG04}
    Newman, M.E.J., Girvan, M. (2004). Finding and evaluating community structure in networks. \textit{Physical Review E}, 69(2), 026113. 
\bibitem{BDG08}
    Brandes, U., Delling, D., \& Gaertler, M. (2008). On Modularity Clustering. \textit{IEEE Transactions on Knowledge and Data Engineering}, 20(2), 172--188.
\bibitem{BGLL08}
    Blondel, V., Guillaume, J., Lambiotte, R., \& Lefebvre, E. (2008). Fast unfolding of communities in large networks. \textit{Journal of Statistical Mechanics: Theory and Experiment}, P10008.
\bibitem{TWE19}
    Traag, V.A., Waltman, L., Van Eck, N.J. (2019). From Louvain to Leiden: guaranteeing well-connected communities. \textit{Scientific reports}, 9, Article 5233. 
\bibitem{Sah23}
    Sahu, S. (2023). GVE-Leiden: Fast Leiden Algorithm for Community Detection in Shared Memory Setting. \textit{arXiv preprint}.
\bibitem{RC17}
    Rossetti, G., Cazabet, R. (2017). Community Discovery in Dynamic Networks: A Survey. \textit{ACM Computing Surveys}, 51(2), 1--37. 
\bibitem{CZW23}
    Cai, L., Zhou, J., Wang, D. (2023). Improving temporal smoothness and snapshot quality in dynamic network community discovery using NOME algorithm. \textit{PeerJ Computer Science}, 9, e1477.
\bibitem{HKT17}
    Halappanavar, M., Lu, H., Kalyanaraman, A., \& Tumeo, A. (2017). Scalable static and dynamic community detection using Grappolo. \textit{Proc. HPEC}, 1--6.
\bibitem{LHK14}
    Lu, H., Halappanava, M., \& Kalyanaraman, A. (2015). Parallel Heuristics for Scalable Community Detection. \textit{Parallel Computing}, 47, 19--37.
\bibitem{HHC22}
    Hanauer, K., Henzinger, M., \& Schulz, C. (2022). Recent Advances in Fully Dynamic Graph Algorithms: A Quick Reference Guide. \textit{ACM JEA}, 27, 1--45.
\bibitem{SLX14}
    Shang, J., Liu, L., Xie, F., Chen, Z., Miao, J., Fang, X., \& Wu, C. (2014). A real-time detecting algorithm for tracking community structure of dynamic networks. \textit{arXiv preprint}.
\bibitem{MZW20}
    Ma, Y., Zhao, Y., Wang, J., et al. (2020). Modularity-Based Incremental Label Propagation Algorithm for Community Detection. \textit{Applied Sciences}, 10(12), 4060.
\bibitem{ZCL19}
    Zhuang, D., Chang, J., \& Li, M. (2019). DynaMo: Dynamic community detection by incrementally maximizing modularity. \textit{IEEE TKDE}, 33(5), 1934--1945.
\bibitem{Seif20}
    Seifikar M., Farzi S., Barati M. (2020). C-Blondel: An Efficient Louvain-Based Dynamic Community Detection Algorithm. \textit{IEEE Transactions on Computational Social Systems}, 7(2), 308--318.
\bibitem{XCS13}
    Xie, J., Chen, M., \& Szymanski, B.K. (2013). LabelRankT: incremental community detection in dynamic networks via label propagation. \textit{DyNetMM}, 25--32.
\bibitem{Bov22}
    Bovet, A., Delvenne, J.-C., Lambiotte, R. (2022). Flow stability for dynamic community detection. \textit{Science Advances}, 8(19).
\bibitem{Sun22}
    Sun, Z., Sun, Y., Chang, X., et al. (2022). Dynamic community detection based on the Matthew effect. \textit{Physica A}, 597, 127315.
\bibitem{Mazza23}
    Mazza, M., Cola, G., Tesconi, M. (2023). Modularity-based approach for tracking communities in dynamic social networks. \textit{Knowledge-Based Systems}, 281, 111067.
\bibitem{Conrad25}
    Conrad, N.D., Tonello, E., Zonker, J., et al. (2025). Detection of dynamic communities in temporal networks with sparse data. \textit{Applied Network Science}, 10, Article 1.
\bibitem{Blaskovic25}
    Bla{\v s}kovi{\'c}, F., Conrad, T.O.F., Klus, S., et al. (2025). Random walk based snapshot clustering for detecting community dynamics in temporal networks. \textit{Scientific Reports}, 15, 24414.
\bibitem{ZK21}
    Zarayeneh, N., Kalyanaraman, A. (2021). Delta-Screening: A Fast and Efficient Technique to Update Communities in Dynamic Graphs. \textit{IEEE TNSE}, 8(2), 1614--1629.
\bibitem{SLe24}
    Sahu, S. (2024). A Starting Point for Dynamic Community Detection with Leiden Algorithm. \textit{arXiv preprint}.
\bibitem{SLu24}
    Sahu, S. (2024). DF Louvain: Fast Incrementally Expanding Approach for Community Detection on Dynamic Graphs. \textit{arXiv preprint}.
\bibitem{DKS26}
    Douadi A., Kamel N., Sais L. (2026). Incremental Similarity-Based Label Propagation Algorithm for Dynamic Community Detection. \textit{Concurrency and Computation: Practice and Experience}, 38(2).
\bibitem{SKB25}
    Sahu, S., Kothapalli, K., Banerjee, D.S. (2025). Parallel Multicore Algorithms for Community Detection in Dynamic Graphs. \textit{IJNC}, 15(1), 2--31.
\bibitem{Sattar25}
    Sattar, N.S., Ibrahim, K.Z., Buluc, A., \& Arifuzzaman, S. (2025). DyG-DPCD: A Distributed Parallel Community Detection Algorithm for Large-Scale Dynamic Graphs. \textit{International Journal of Parallel Programming}, 53, Article 4.
\bibitem{HK26}
    Harutyunyan H.A., Kamalipour P. (2026). Spider Community Detection: Seeded Geodesic Expansion with Modularity-Guided Refinement and Greedy Merge Matching. \textit{Computers}, 15(2), 83.
\bibitem{CCL23}
    Chunaev, P. (2020). Community detection in node-attributed social networks: A survey. \textit{Computer Science Review}, 37, 100286.
\bibitem{LXZ23}
    Liu, Y., Xia, J., \& Zhou, S. (2023). A Survey of Deep Graph Clustering: Taxonomy, Challenge, Application, and Open Resource. \textit{arXiv preprint}.
\bibitem{ZXY20}
    Zhang, Y., Xiong, Y., \& Ye, Y. et al. (2020). SEAL: Learning Heuristics for Community Detection with Generative Adversarial Networks. \textit{Proc. KDD}, 1103--1113.
\bibitem{DSD24}
    Devesh, K.V.V., Sreesankar, S., \& Dinesan, K. et al. (2024). Analyzing GNN Models for Community Detection Using Graph Embeddings: A Comparative Study. \textit{Proceedings of ICCCNT}.
\bibitem{JLL23}
    Ji, S., Lu, X., \& Liu, M. et al. (2023). Community-based Dynamic Graph Learning for Popularity Prediction. \textit{Proc. KDD}, 930--940.
\bibitem{TPP23}
    Tsitsulin, A., Palowitch, J., Perozzi, B., \& M{\"u}ller, E. (2023). Graph clustering with graph neural networks. \textit{JMLR}, 24(1), 5809--5829.
\bibitem{SGH09}
    Scarselli, F., Gori, M., Tsoi, A.C., Hagenbuchner, M., \& Monfardini, G. (2009). The graph neural network model. \textit{IEEE Transactions on Neural Networks}, 20(1), 61--80.
\bibitem{ZXZ20}
    Zhang, T., Xiong, Y., \& Zhang, J. et al. (2020). CommDGI: Community Detection Oriented Deep Graph Infomax. \textit{Proc. CIKM}, 1843--1852.
\bibitem{LYD24}
    Liu, C., Yang, Y., \& Ding, Y. et al. (2024). DAG: Deep Adaptive and Generative K-Free Community Detection on Attributed Graphs. \textit{Proc. KDD}, 5454--5465.
\bibitem{G10}
    Good B.H., et al. (2010). Performance of modularity maximization in practical contexts. \textit{Physical Review E}, 81(4).
\bibitem{FH16}
    Fortunato, S., Hric D. (2016). Community detection in networks: A user guide. \textit{Physics Reports}, 659, 1--44.
\bibitem{YL15}
    Yang, J., Leskovec, J. (2015). Defining and evaluating network communities based on ground-truth. \textit{Knowledge and Information Systems}, 42(1), 181--213.
\bibitem{YAT16}
    Yang, Z., Algesheimer, R., \& Tessone, C.J. (2016). A Comparative Analysis of Community Detection Algorithms on Artificial Networks. \textit{Scientific Reports}, 6, Article 30750.
\bibitem{Rustamaji24}
    Rustamaji, H.C., Kusuma, W.A., Nurdiati, S., Batubara, I. (2024). Community detection with greedy modularity disassembly strategy. \textit{Scientific Reports}, 14, 4694.
\bibitem{TTB24}
    Taibi, S., Toumi, L. \& Bouamama, S. (2024). Complex network community discovery using fast local move iterated greedy algorithm. \textit{The Journal of Supercomputing}, 81(1).
\bibitem{LLC24}
    Liu, Y., Li, J., \& Chen, Y. et al. (2024). Revisiting Modularity Maximization for Graph Clustering: A Contrastive Learning Perspective. \textit{Proc. KDD}, 1968--1979.
\bibitem{BKH24}
    Bhowmick, A., Kosan, M., \& Huang, Z. et al. (2024). DGCLUSTER: a neural framework for attributed graph clustering via modularity maximization. \textit{Proc. AAAI}, 11069--11077.
\bibitem{QZG24}
    Qin, M., Zhang, C., \& Gao, Y. et al. (2024). Pre-train and Refine: Towards Higher Efficiency in K-Agnostic Community Detection without Quality Degradation. \textit{Proc. KDD}, 2467--2478.
\bibitem{QZG24HPEC}
    Qin, M., Zhang, C. \& Gao, Y. et al. (2024). Towards Faster Graph Partitioning via Pre-training and Inductive Inference. \textit{Proc. HPEC}.
\bibitem{ZPS25}
    Zhang, J., Peng, H., \& Sun, L. et al. (2025). Unsupervised Graph Clustering with Deep Structural Entropy. \textit{Proc. KDD}, 3752--3763.
\bibitem{GPT25}
    Gkartzios, C., Pitoura, E., Tsaparas, P. (2025). Modularity-Fair Deep Community Detection. \textit{Proc. ICDM}, 287--296.
\bibitem{LYZ25}
    Lin, X., Yang, R., \& Zheng, H. et al. (2025). Spectral Subspace Clustering for Attributed Graphs. \textit{Proc. KDD}, 789--799.
\bibitem{KZL24}
    Kong, D., Zhang, A., \& Li, Y. (2024). Learning Persistent Community Structures in Dynamic Networks via Topological Data Analysis. \textit{Proc. AAAI}.
\bibitem{Brabant25}
    Brabant, V., Bonifati, A., \& Cazabet, R. (2025).
    Discovering Communities in Continuous-Time Temporal Networks by Optimizing L-Modularity.
    \textit{Proc. ICDM}.
\bibitem{ZGR25}
    Zhao Z., Gan, J., \& Ruan, B. et al. (2025). Dynamic Structural Clustering Unleashed: Flexible Similarities, Versatile Updates and for All Parameters. \textit{Proc. KDD}, 3980--3991.
\end{thebibliography}
\endgroup


\end{document}
